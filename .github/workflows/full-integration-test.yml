name: Full Integration Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  full-integration-test:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.4'
        bundler-cache: false

    - name: Install VS Code for file associations
      run: brew install --cask visual-studio-code

    - name: Run dotfiles
      id: run
      timeout-minutes: 10
      env:
        DEBUG: true
        CI: true
        NONINTERACTIVE: true
      run: |
        set -o pipefail
        cd ${{ github.workspace }}
        chmod +x bin/dotf
        ./bin/dotf run 2>&1 | tee output.log

    - name: Confirm success
      run: echo "✅ dotf run exited successfully"

    - name: Start fish shell and check for errors
      run: |
        echo "Starting fish shell to check for initialization errors..."
        /opt/homebrew/bin/fish -c 'echo "Fish shell initialized successfully"' 2>&1 | tee fish-init.log
        if [ $? -ne 0 ]; then
          echo "❌ Fish shell initialization failed"
          cat fish-init.log
          exit 1
        fi
        echo "✅ Fish shell started without errors"

    - name: Upload logs
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: logs
        path: |
          output.log
          fish-init.log
        retention-days: 7
