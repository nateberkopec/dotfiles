# Support macOS admin, macOS non-admin, and Debian

The supported targets are macOS with admin rights, macOS without admin rights, and Debian/Ubuntu with a sudo-capable user. Support means the repo should converge as much as practical on each target, not that every target behaves identically; Steps may be platform-specific, admin-aware, or skip with a notice when a target cannot safely perform an action.
